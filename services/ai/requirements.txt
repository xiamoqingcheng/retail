# retail-ai 运行依赖（版本对齐本机已验证的可用环境）。
# 注：ultralytics 会拉入 torch（约数 GB），CPU/GPU 构建取决于宿主机；
# 如需 CPU-only，请先按官方说明单独安装匹配的 torch，再 pip install -r。
fastapi==0.115.0
uvicorn==0.46.0
pydantic==2.12.5
numpy==2.2.6
opencv-python==4.11.0.86
pillow==12.1.1
ultralytics==8.3.0
# 文本向量化（语义/混合检索）。torch 复用 ultralytics 已装版本；版本对齐本机已验证环境。
sentence-transformers==5.5.1
# 模型来源：HuggingFace 在国内常被墙，改用 ModelScope（魔搭）下载 embedding 模型。
modelscope>=1.20
